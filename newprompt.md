# MASTER EXECUTION PROMPT: REDESIGN AND SHIP THE SCRATCH CHEMISTRY SIMULATOR

## AGY execution contract

This prompt is addressed to AGY. AGY is the sole implementation and delivery agent for this task. AGY must perform the repository inspection, coding, asset generation, `.sb3` construction, testing, validation, Git operations, commit, and push described below. Do not hand the work back to the user or ask the user to create files, write Scratch blocks, generate assets, run build steps, or finish any remaining implementation. Do not merely explain what another agent should do: execute the work directly in the current repository.

Credit boundary: if AGY runs out of credits, reaches a credit limit, or can no longer continue execution, stop immediately at that point. Do not claim completion, validation, commit, or push. Preserve all work already written, report the exact stopping point and unfinished steps, and clearly state that the task is incomplete. Never compensate for exhausted credits by inventing results or asking the user to assume that an unverified step succeeded.

You are the implementation agent responsible for creating and shipping a complete, original, polished Scratch chemistry simulation. Work directly in the current repository and finish the work end to end. Do not answer with a tutorial, pseudocode, mockup, or a list of suggestions. Inspect the existing project, change the files, build the real `.sb3`, validate it, test it, and publish the completed changes.

## Existing project context

The previous version was designed and generated by AGY. Treat the current repository as the old version and its working material, not as disposable scaffolding. Before editing, inspect:

- `instructions.md` as the original functional specification.
- `build_full_project.py`, `pack_sb3.py`, `clean_meta.py`, `db_chemistry.py`, `verify_reactions.py`, and all related generators.
- `assets/`, `knowledge/`, `etc/`, `sb3/`, and `package.json`.
- Both existing `.sb3` files, their ZIP contents, `project.json`, targets, blocks, assets, and metadata.

If the `agy` terminal command exists, inspect it with `agy --help` and use it where it is useful for understanding or improving the old version. If it does not exist, continue with the repository tools and record that fact. Never invent the output of an unavailable command. The old version is a reference for behavior, chemistry coverage, assets, and implementation lessons; it must not constrain the new visual design.

## Primary outcome

Create a genuine, valid, runnable Scratch 3 project for a vacuum chemistry sandbox in which users can add, drag, inspect, combine, ionize, discover, delete, save, and load atoms, molecules, and ions. The result must feel like a deliberate human-designed scientific instrument, not an AI-generated prototype or a collection of decorative buttons.

The primary deliverable is a working `.sb3`, accompanied by the source/data/assets needed to reproduce it, validation results, known limitations, and the committed and pushed repository changes.

## Non-negotiable build rules

1. Understand and preserve the real Scratch 3 `.sb3` format. It is a ZIP containing valid `project.json` and correctly referenced assets.
2. Use real Scratch targets, sprites, stage data, blocks, IDs, opcodes, inputs, fields, variables, lists, broadcasts, clones, costumes, sounds, monitors where needed, custom procedures, and procedure mutations.
3. Do not rename text to `.sb3`, fabricate opcodes, use invalid JSON, or claim a feature works when it does not.
4. Reuse sound engineering and useful data from the old version when appropriate, but write the redesign as an original implementation. Do not copy protected source, graphics, or sounds from unrelated projects.
5. Keep the architecture maintainable and data-driven. Use clear names such as `selectedEntity`, `simulationPaused`, `reactionType`, `moleculeId`, and `currentCharge`; never use meaningless names such as `thing2`, `temp2`, or `AIData123`.
6. Generate required assets yourself. Every visual or sound must serve a real interface or simulation purpose.
7. If standard Scratch cannot provide arbitrary persistence or another requested capability, implement the strongest compatible alternative, document the limitation, and never fake success.

## Complete GUI redesign brief

The GUI must be redesigned from first principles rather than recolored or rearranged. The old version was AGY-designed; use it to identify working behavior and weak points, then make a visibly new version with a coherent visual language.

### Visual direction

Design a restrained, editorial scientific instrument: a dark vacuum chamber with carefully controlled light, high-contrast scientific labels, restrained accent colors, and strong spacing. Avoid generic AI dashboard patterns, default Scratch UI, random neon colors, purple-on-white templates, excessive gradients, giant pill buttons, ornamental cards, and meaningless decoration. Use a small intentional palette for chamber, panels, text, neutral species, positive ions, negative ions, UV, cosmic rays, energy, warnings, and success states. Typography, icon proportions, stroke weights, corner radii, spacing, and states must be consistent.

The chamber must be the primary visual focus. The surrounding controls must support experimentation without covering the experiment. Use clear hierarchy: compact tool rail, spacious simulation field, focused inspector, and a browsable discovery view. The interface must remain legible on the Scratch stage at the intended aspect ratio and degrade gracefully when space is limited.

### Required UI regions

- A branded simulation header with project title, run state, discovery count, and a compact status summary.
- A large vacuum chamber with subtle depth, restrained particle ambience, readable boundaries, and no visual clutter.
- A real add-material workflow with search or categories and a deliberately organized element/species palette. It must not be a flat chaotic list.
- A tool rail or toolbar containing real controls for add, delete, inspect, UV, freeze time, audio, save, load, discoveries, and help.
- Clear active, hover, pressed, disabled, and confirmation states for every interactive control.
- A selected-entity inspector showing name, formula, charge, composition, bonding or reaction information, and discovery status.
- A discovery browser separating atoms, neutral molecules, radicals, and ions, with readable formulas and useful filtering or grouping.
- A compact event/status feed for reaction results, invalid combinations, ionization, discoveries, save/load feedback, and limitations.
- A concise first-run onboarding flow that teaches add, drag, combine, delete, inspect, UV, freeze, and discoveries without permanently filling the screen.
- Confirmation or undo protection for destructive collection deletion and other risky actions.

### GUI quality requirements

- Every control must change real simulation or application state.
- Icons must be consistent and understandable; add labels or tooltips where an icon alone is ambiguous.
- Never use placeholder text, fake panels, dead menus, empty cards, or decorative controls.
- Do not put cards inside cards or turn every section into a floating rounded container.
- Make long names and formulas fit without overlap; use wrapping, truncation, or dynamic layout deliberately.
- Make the UI keyboard-accessible where Scratch permits it and preserve clear mouse/touch-like pointer interactions.
- Use meaningful motion: load-in reveal, selection feedback, reaction burst, discovery notification, UV activation, and freeze transition. Avoid constant distracting animation.
- Create original atom, molecule, ion, photon, cosmic-ray, reaction, button, icon, panel, and effect assets. Do not use emoji as a substitute for a finished icon system.
- Perform a visual comparison against the old version and remove any element that still reads as a generic generated prototype.

## Simulation and chemistry requirements

Build a real shared entity system. Each entity should have coherent identity and state including element or species, formula, charge, position, velocity where useful, constituent atoms, bonds or relationships where useful, molecule identity, selected state, interaction state, and lifecycle state. Molecules cannot be sprites that only display a label. Ions must have real charge state.

Support all elements needed by the chemistry database, including at minimum H, He, Li, Be, C, N, O, F, Ne, Na, Si, P, S, Cl, Ar, K, Ca, Br, and I, plus any additional required element.

Attempt to support every one of these neutral species and radicals as actual structured species, not fake labels:

`H2`, `H2O`, `O2`, `Li2`, `Be2`, `N2`, `F2`, `CN`, `NH•`, `NO•`, `HCl`, `Cl2`, `NaCl`, `BeCl2`, `ClO2`, `HOCl`, `Cl2O`, `KCl`, `CaCl2`, `CO`, `CO2`, `CH•`, `CH2•`, `CH3•`, `CH4`, `C2H6`, `NH2•`, `NH3`, `HCN`, `Na2CO3`, `H2CO`, `HCO•`, `C2H4`, `C2H2`, `CH3CHO`, `CH3COOH`, `HS•`, `H2S`, `SO2`, `SO3`, `H2SO4`, `NaHSO4`, `KHSO4`, `S2`, `S3`, `S4`, `N4`, `S4N4`, `S2Cl2`, `NH4Cl`, `SiO`, `SiO2`, `Na2SiO3`, `SiH•`, `SiH2•`, `SiH3•`, `SiH4`, `Si2H6`, `K2SiO3`, `P2`, `P4`, `PH•`, `PH2•`, `PH3`, `P4O10`, `H3PO4`, `PN`, `Na3PO4`, `KH2PO4`, `O3`, `H2O2`, `Br2`, `I2`, `HF`, `HBr`, `HI`, `NaF`, `KF`, `NaBr`, `KBr`, `NaI`, `KI`, `HArF`, `CaO`, `Ca(OH)2`, `CaCO3`, `NaOH`, and `OF2`.

Support these ions with correct underlying charge:

`H-`, `H+`, `H3+`, `OH-`, `H2+`, `NH+`, `N+`, `CN-`, `HeH+`, `Cl-`, `Na+`, `CO3-2`, `HCO3-`, `S-2`, `HSO4-`, `H3O+`, `SO4-2`, `K+`, `SiO3-2`, `H2PO4-`, `HPO4-2`, and `PO4-3`.

Create a structured species database and centralized reaction database containing formula, charge, composition, bonds/relationships where useful, compatible reactants, products, reaction type, energy behavior, UV requirements, ionization behavior, visual identity, and discovery metadata. Avoid hundreds of unrelated scripts. Reactions must modify actual entity state and must not repeat every frame after one contact.

## Required interactions and systems

Implement and test all of the following:

- Dragging and selection of atoms and molecules with responsive pointer behavior.
- Proximity detection and centralized reaction evaluation on release or deliberate collision.
- Add mode with organized element/material selection.
- Delete mode with visible state, feedback, and accidental-action protection.
- Molecule creation with coherent constituent atoms and bonds.
- Ion creation and charge-preserving transformations.
- Exothermic reactions with event-linked orange energy effects and, where appropriate, photons.
- Cosmic rays as fast colored particles with real interaction rules that can create ions.
- UV control whose state changes UV-dependent reactions and has a distinct, restrained visual state.
- Freeze-time control that stops movement and relevant timers while preserving the simulation and correctly resumes it.
- Speaker/volume control that affects actual project audio.
- Information inspector for selected atoms, molecules, radicals, and ions.
- Discovery system with distinct neutral, radical, ion, and atom collections.
- Save/load behavior using the strongest standard Scratch-compatible persistence available.
- D-key behavior for discovery reset only when intentionally confirmed or otherwise protected from accidental deletion.
- Event/status feedback for successes, invalid reactions, discoveries, mode changes, and errors.

Use custom blocks where they genuinely clarify initialization, spawning, removal, entity updates, reaction checks, molecule/ion creation, UI updates, save, and load. Do not create custom blocks merely to inflate the project.

## Performance and architecture

Use modular systems appropriate for Scratch: entity management, atom/molecule/ion state, bonding, reaction engine, input, spawning/deletion, cosmic rays, UV, photons, time, audio, discovery, information, save/load, and UI. Use controlled update loops, bounded clone counts, cached calculations, efficient collision checks, cleanup of dead entities, and event-driven UI where possible. Profile or reason about expensive paths instead of ignoring slowdown. Keep the chamber responsive with a practical entity limit and a visible, honest limit message if necessary.

## Verification loop

Iterate for every major subsystem: plan, implement, test, inspect failure, fix the root cause, and regression-test. Before finalizing:

1. Validate the ZIP structure and `project.json`.
2. Validate every target, block, variable, list, broadcast, custom procedure, mutation, costume, sound, and asset reference.
3. Run the repository’s chemistry and project validation scripts, adapting only when necessary to the redesigned project.
4. Open and exercise the actual generated `.sb3` if the environment supports it.
5. Test startup, green flag, add, drag, selection, combine, invalid combinations, delete, keyboard controls, UV, cosmic rays, photons, exothermic feedback, freeze, audio, inspector, discoveries, save, load, reset protection, and performance.
6. Perform a dedicated visual review at the intended stage size. Check hierarchy, spacing, contrast, readability, overlap, formula rendering, active states, empty states, error states, and whether the project looks intentionally made by a skilled human.
7. Fix failures and rerun validation. Do not stop after the first successful generation.

Do not finish with fake functionality, unused UI, missing assets, corrupt clones, invalid references, dead variables, accidental infinite loops, or an interface that merely looks polished in screenshots.

## GitHub delivery requirement

After the implementation and validation are complete:

1. Run `git status --short` and inspect the diff.
2. Keep unrelated user changes intact and stage only the files belonging to this redesign and its required generated outputs.
3. Verify the configured remote and current branch. Do not expose credentials or secrets.
4. Create a clear commit, for example: `Redesign chemistry simulator GUI and ship validated SB3`.
5. Push the commit to the correct GitHub remote and branch.
6. Verify that the local branch and remote are synchronized, and report the commit hash and pushed branch.
7. If authentication, network, branch protection, or remote configuration blocks the push, do not pretend it succeeded. Report the exact blocker and leave the changes committed locally if possible.

## Final response

Report only after the work is actually complete. Summarize the redesigned experience, the generated `.sb3` path, the important validation commands and results, the commit hash, the pushed branch/remote, and any genuine limitations. Do not claim that a feature, test, commit, or push happened unless you verified it.

START NOW.